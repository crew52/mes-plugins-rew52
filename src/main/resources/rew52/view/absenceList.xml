<view xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://schema.qcadoo.org/view" xsi:schemaLocation="http://schema.qcadoo.org/view http://schema.qcadoo.org/view.xsd" name="absenceList" modelName="absenceDto" menuAccessible="true">

    <component type="window" name="window" reference="window">
        <ribbon>
            <template name="standardGridTemplate"/>
        </ribbon>

        <component type="grid" name="absences" reference="grid">
            <!-- Các cột hợp lệ theo model absenceDto -->
            <option type="column" name="employeeNumber" fields="employeeNumber" link="true"/>
            <option type="column" name="employeeName" fields="employeeName"/>
            <option type="column" name="absenceType" fields="absenceType"/>
            <option type="column" name="startDate" fields="startDate"/>
            <option type="column" name="endDate" fields="endDate"/>
            <option type="column" name="reason" fields="reason"/>
            <option type="column" name="status" fields="status"/>

            <!-- Metadata -->
            <option type="column" name="createUser" fields="createUser"/>
            <option type="column" name="createDate" fields="createDate"/>
            <option type="column" name="updateUser" fields="updateUser"/>
            <option type="column" name="updateDate" fields="updateDate"/>

            <!-- Tùy chọn bảng -->
            <option type="order" column="createDate" direction="desc"/>
            <option type="searchable" value="employeeNumber,employeeName,absenceType"/>
            <option type="orderable" value="createDate"/>

            <option type="correspondingView" value="rew52/absenceDetails"/>
            <option type="correspondingComponent" value="form"/>

            <option type="fullscreen" value="true"/>
            <option type="multiselect" value="true"/>
            <option type="fixedHeight" value="true"/>
            <option type="header" value="true"/>
        </component>
    </component>
</view>